# =============================================================================
# Vouch — Web & API Runtime Dependencies (AWS Elastic Beanstalk)
# Excludes heavy ML/NLP wheels (torch, transformers, accelerate) to ensure fast,
# reliable Elastic Beanstalk deployments without memory or download failures.
# =============================================================================

# Core Web Dashboard & Production WSGI Server
flask>=3.0.0
gunicorn>=22.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# GitHub App Authentication (RS256 JWT + Installation Access Tokens)
PyJWT>=2.8.0
cryptography>=42.0.0


# Repository Mining & Feature Engineering
GitPython>=3.1.40
pandas>=2.2.0
pyarrow>=15.0.0
numpy>=1.26.0

# Machine Learning & Scorer Models (Lightweight runtime inference)
scikit-learn>=1.4.0
xgboost>=2.0.0

# AWS Cloud & DynamoDB Integration
boto3>=1.34.0
